"""Grafische Oberfläche von AutoCut (Tkinter).

Die Oberfläche kennt nur ``autocut.pipeline`` – die gesamte Verarbeitung
liegt im Paket ``autocut``. Dadurch lässt sich die Logik weiterentwickeln,
ohne die Oberfläche anzufassen (und umgekehrt).
"""
